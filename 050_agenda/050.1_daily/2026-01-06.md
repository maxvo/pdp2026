# Distribuição de horas
```dataviewjs
```

```dataviewjs
let keyDeliveryDuration = {};

// First pass: Collect all durations
dv.current().file.tasks.map(task => {
    const timeRange = task.text.match(/(\d{2}):(\d{2}) - (\d{2}):(\d{2})/);
    if (!timeRange) return null;
    
    const startMinutes = parseInt(timeRange[1]) * 60 + parseInt(timeRange[2]);
    const endMinutes = parseInt(timeRange[3]) * 60 + parseInt(timeRange[4]);
    const duration = endMinutes - startMinutes;

    const keyDeliveryMatch = task.text.match(/\[\[([^\]]+)\]\]/);
    const keyDelivery = keyDeliveryMatch ? keyDeliveryMatch[1] : "Entrega não cadastrada"

    if (!keyDeliveryDuration[keyDelivery]) {
        keyDeliveryDuration[keyDelivery] = 0;
    }
    keyDeliveryDuration[keyDelivery] += duration;
});

// Calculate total time across all key deliveries
const totalTime = Object.values(keyDeliveryDuration).reduce((sum, duration) => sum + duration, 0);

// Prepare table with percentage calculation
let table = [];
for (const [keyDelivery, totalDuration] of Object.entries(keyDeliveryDuration)) {
    const percentage = totalDuration / totalTime * 100;
    table.push([
        keyDelivery, 
        totalDuration,
        `${percentage.toFixed(1)}%`  // Format to 1 decimal place
    ]);
}

table.sort((a, b) => b[1] - a[1]);  // Sort by duration descending

// Create table with new percentage column
dv.table(
    ["Key Delivery", "Total Minutes", "% of Total"], 
    table
);
```

# Agenda

- [x] 10:00 - 11:10 Como tornar nossa área o melhor local para se trabalhar no banco [keyDelivery:: [[Agendas da gerência]]]
- [x] 16:00 - 16:30 GT Ciclo de vida de projetos de  EA [keyDelivery:: [[Agendas da gerência]]]
- [x] 09:20 - 09:30 Criar o modulo de desenvolvimento da operação [keyDelivery:: [[Criação do Analytics de Operação]]]
- [x] 15:20 - 16:00 Desenvolver o finops das contas producers [keyDelivery:: [[Criação do Analytics de Operação]]]
- [x] 09:10 - 09:20 Conversa com o Caio [keyDelivery:: [[Roadmap de entregas técnicas alinhadas ao negócio]]]
- [x] 09:30 - 09:40 Apoio com dúvidas tablify da Trovão [keyDelivery:: [[Especialista do banco]]]
- [x] 14:00 - 14:50 Apoio com o tablify e munin [keyDelivery:: [[Especialista do banco]]]
- [x] 11:20 - 12:00 Criar modulo de desenvolvimento na operação [keyDelivery:: [[Criação do Analytics de Operação]]]
- [x] 14:50 - 15:00 Apoio atividades do Caleb [keyDelivery:: [[Agendas da gerência]]]
- [x] 16:30 - 17:00 Criar lambda de finops da conta producer [keyDelivery:: [[Criação do Analytics de Operação]]]
- [x] 17:00 - 17:20 Book Registry com a Thais [keyDelivery:: [[Camada semântica]]]

# Anotações
